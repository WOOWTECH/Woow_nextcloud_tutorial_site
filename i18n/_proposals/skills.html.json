{
  "file": "skills.html",
  "_note": "skills.html #spec holds a whole SKILL.md example inside a frozen <pre class=\"code-block\"> (gate B), so it is not edited in en/. policy.json marks skills.html 'translate' with the brief 'any prompt demanding Traditional Chinese output becomes English-only'; this block is a spec example rather than a prompt, and it demands no output language, so nothing about output language is added. The English wording is taken verbatim from /home/user/out/terms_nextcloud.txt (code placeholders, 'skills.html #spec (SKILL.md)'). The YAML keys (name, description), the skill id nextcloud-share-auditor, the API names (OCS API, OCS Shares API), the Markdown heading levels and the 4 numbered steps / 3 safety bullets are unchanged, so the block keeps its shape and the counts the zh source has. Full-width punctuation becomes ASCII; 「預覽清單」 becomes a plain-quoted \"preview list\", 影響 N 筆 becomes (N affected), 影響範圍 becomes 'blast radius', and App 密碼 becomes 'app password' per the glossary. The <pre> tag carries the same class attribute, so gate A's tag skeleton is unchanged. The block occurs exactly once in en/skills.html. Prose on the page already refers to the block's \"Steps\" and \"Safety\" sections in English (#faq), so accepting this proposal makes the page self-consistent.",
  "entries": [
    {
      "unit": "#spec",
      "zh": "<pre class=\"code-block\">---\nname: nextcloud-share-auditor\ndescription: 掃描 Nextcloud 分享、標出風險項並可批次收回，附乾跑模式\n---\n# 用途\n用 OCS API 盤點所有分享，找出無密碼／無期限／公開的風險項。\n\n## 步驟\n1. 呼叫 OCS Shares API 取得所有分享\n2. 標記風險項，產生「預覽清單」（影響 N 筆）\n3. 等使用者確認後才批次補密碼／設期限／停用\n4. 回報結果與例外\n\n## 安全\n- 永不直接刪檔，只調整分享設定\n- 批次操作前必須先回報影響範圍\n- 一律用 App 密碼</pre>",
      "en": "<pre class=\"code-block\">---\nname: nextcloud-share-auditor\ndescription: Scan Nextcloud shares, flag risky ones and revoke them in bulk, with a dry-run mode\n---\n# Purpose\nUse the OCS API to inventory every share and find the ones with no password, no expiry, or public access.\n\n## Steps\n1. Call the OCS Shares API to fetch every share\n2. Flag the risky ones and produce a \"preview list\" (N affected)\n3. Only after the user confirms, bulk-add passwords, set expiry dates or disable them\n4. Report the results and any exceptions\n\n## Safety\n- Never delete a file directly, only change share settings\n- Always report the blast radius before a bulk operation\n- Always use an app password</pre>"
    }
  ]
}
